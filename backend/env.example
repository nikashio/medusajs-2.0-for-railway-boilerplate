# Environment configuration for Medusa 2.0
# Copy this to .env and update the values for your local development

NODE_ENV=development

# Database Configuration
# For local development with Docker:
DATABASE_URL=postgresql://medusa:medusa@localhost:5432/medusa
# For Railway production:
# DATABASE_URL=postgresql://postgres:password@postgres.railway.internal:5432/railway

# Redis Configuration (Optional - falls back to simulated redis)
REDIS_URL=redis://localhost:6379

# CORS Configuration
ADMIN_CORS=http://localhost:7000,http://localhost:7001,https://docs.medusajs.com
STORE_CORS=http://localhost:8000,https://docs.medusajs.com
AUTH_CORS=http://localhost:7000,http://localhost:7001,https://docs.medusajs.com

# Security
JWT_SECRET=supersecret
COOKIE_SECRET=supersecret

# Admin User (Optional - for seeding)
# MEDUSA_ADMIN_EMAIL=admin@yourmail.com
# MEDUSA_ADMIN_PASSWORD=supersecret

# Payment Provider Configuration (Optional)
# STRIPE_API_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...

# Authorize.net Configuration (Optional)
# AUTHORIZE_NET_API_LOGIN_ID=your_api_login_id
# AUTHORIZE_NET_TRANSACTION_KEY=your_transaction_key
# AUTHORIZE_NET_ENVIRONMENT=sandbox
# AUTHORIZE_NET_CAPTURE=true

# Email Provider Configuration (Optional)
# Choose either SendGrid OR Resend, not both

# SendGrid Configuration
# SENDGRID_API_KEY=SG....
# SENDGRID_FROM=noreply@yourstore.com

# Resend Configuration
# RESEND_API_KEY=re_...
# RESEND_FROM_EMAIL=noreply@yourstore.com

# MinIO Storage Configuration (Optional - falls back to local storage)
# MINIO_ENDPOINT=localhost:9000
# MINIO_ACCESS_KEY=minioadmin
# MINIO_SECRET_KEY=minioadmin
# MINIO_BUCKET=medusa-media

# Meilisearch Configuration (Optional)
# MEILISEARCH_HOST=http://localhost:7700
# MEILISEARCH_ADMIN_KEY=your-admin-key
